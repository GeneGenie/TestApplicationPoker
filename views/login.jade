extends layout

block content
    form#loginForm(action="/user/login",method="POST")
        span Login
        input(type="text",name='login')
        span Password
        input(type="password",name='pwd')
        button(type='submit') Enter
    if error
        div.error Login or Password is wrong.